# 后端 API 地址
# 本地开发：http://127.0.0.1:8000
# Cloudflare Tunnel 部署（每次启动会变）：https://xxxx.trycloudflare.com
VITE_API_BASE_URL=http://127.0.0.1:8000

# ⚠️ 安全提示：访问令牌不要在 build-time 设置！
# 前端部署到 GitHub Pages 后，所有 VITE_ 环境变量会被打包进 JS 文件，任何人可见。
# 正确做法：打开页面后，在右上角「访问令牌」按钮中手动输入，令牌保存在浏览器 localStorage。
# 后端真实令牌仅保存在本机 backend/.env，不会上传 GitHub。
#
# 旧配置（已废弃，仅用于本地开发临时调试）：
# VITE_APP_ACCESS_TOKEN=
